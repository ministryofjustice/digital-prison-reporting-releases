release:
  project: hmpps-dpr-postgres-tickle-lambda
  release_type: gradle
  release_category: backend
  tag: v0.0.1 # tbc
  extra_args:
    refresh_lambda: false # tbc
    refresh_function: dpr-postgres-tickle-function
  s3_sync_args:
    app_dir: ./project
    bucket_prefix: dpr-artifact-store
    src_artifacts_path: build/libs/
    sync_args: '--exclude \"*\" --include \"*-all*jar\"'
    is_gradle_build: true